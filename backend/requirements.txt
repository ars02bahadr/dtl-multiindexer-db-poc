# Flask ve Extensions
Flask>=3.0.0
flask-cors>=4.0.0
flask-restx>=1.3.0
Werkzeug>=3.0.0

# Redis (opsiyonel cache)
redis>=5.0.0

# Blockchain / Ethereum
web3>=6.0.0
eth-account>=0.10.0

# HTTP Client
requests>=2.31.0

# Environment
python-dotenv>=1.0.0

# Production Server
gunicorn>=21.0.0

# Development
pytest>=7.4.0
pytest-flask>=1.3.0

# =============================================
# OpenCBDC Mode için GEREKMEYENLER:
# =============================================
# flask-jwt-extended (kendi wallet auth'umuz var)
# flask-migrate (database yok)
# flask-sqlalchemy (database yok)
# psycopg2-binary (PostgreSQL yok)
# SQLAlchemy (database yok)
